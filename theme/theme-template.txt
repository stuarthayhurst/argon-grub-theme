# GRUB2 gfxmenu Linux theme
# Designed for any resolution

# Global Property
title-text: ""
desktop-image: "background.png"
desktop-color: "#000000"
terminal-font: "Terminus Regular {font_size_template}"
terminal-box: "terminal_box_*.png"
terminal-left: "0"
terminal-top: "0"
terminal-width: "100%"
terminal-height: "100%"
terminal-border: "0"

# Show the boot menu
+ boot_menu {
  left = 30%
  top = 30%
  width = 40%
  height = 50%
  item_font = "{font_name_template} {font_size_template}"
  item_color = "#cccccc"
  selected_item_color = "#ffffff"
  icon_width = {icon_size_template}
  icon_height = {icon_size_template}
  item_icon_space = {item_icon_space_template}
  item_height = {item_height_template}
  item_padding = {item_padding_template}
  item_spacing = {item_spacing_template}
  selected_item_pixmap_style = "select_*.png"
}

# Show a countdown message using the label component
+ label {
  top = 80%
  left = 35%
  width = 30%
  align = "center"
  id = "__timeout__"
  text = "Booting in %d seconds"
  color = "#cccccc"
  font = "{font_name_template} {font_size_template}"
}
